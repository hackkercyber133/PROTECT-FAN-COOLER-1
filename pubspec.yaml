name: app_controller
description: A fan cooler controller app.
publish_to: 'none'
version: 1.2.0+3

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_blue_plus: ^1.32.12
  mqtt_client: ^10.3.1

flutter:
  uses-material-design: true
